name: Test

on:
  push:
    branches: ["master"]

permissions:
  contents: write

env:
  BUILD_TYPE: Release

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install Packages
        run: sudo apt install -y libboost-all-dev libeigen3-dev

      - name: Build Libint
        run: ./script/libint.sh

      - name: Build Libxc
        run: ./script/libxc.sh

      - name: Configure Hazel
        run: cmake -B build -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}}

      - name: Run Tests
        working-directory: build
        run: |
          export CPLUS_INCLUDE_PATH="$PWD/libint/install/include:$PWD/libxc/install/include:$CPLUS_INCLUDE_PATH"
          export LIBRARY_PATH="$PWD/libint/install/lib:$PWD/libxc/install/lib:$LIBRARY_PATH"
          ctest --verbose
